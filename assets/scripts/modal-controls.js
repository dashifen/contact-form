import MicroModal from "micromodal";
import Axios from "axios";

document.addEventListener('DOMContentLoaded', () => {
  const viewLinks = document.querySelectorAll('.view-message');
  viewLinks.forEach((link) => {
    console.log(link);
  })
});
